---
name: openspec-apply
description: Apply an OpenSpec proposal to the codebase.
---

Slash-command-style. Per #172, the user typing `/openspec-apply` triggers
this prompt — Claude inlines the body wrapped in `<command-name>` so we can
detect the invocation from the content sidecar.
