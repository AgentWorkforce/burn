# OpenSpec apply

Apply the most recent OpenSpec proposal to the codebase. Per #172, when the
user types `/openspec-apply` Codex prepends this body to the user message
verbatim — slash-command detection looks for the literal `/openspec-apply`
in the user-turn text.
